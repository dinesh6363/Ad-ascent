# Copy this file to `.env` and fill in the value.
# `.env` is git-ignored — never commit real secrets.

# Resend API key — used by src/pages/api/contact.ts to send form submissions.
# Create one at https://resend.com (free tier). Add the same key in the
# Vercel dashboard: Project → Settings → Environment Variables.
RESEND_API_KEY=
