---
/**
 * Ad-Ascent wordmark — plain text, the full name in the brand gradient.
 */
interface Props {
  class?: string;
}
const { class: className = "" } = Astro.props;
---

<span class={`text-gradient text-[1.2rem] font-bold tracking-tight ${className}`}>Ad-Ascent</span>
